<h1>
  <%= "Book Tickets for #{@event.name} "%>
</h1>

<%= form_with(model: @event, url: event_event_bookings_path(@event), method: :post) do |form| %>
  <% if @event.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this @event from being saved:</h2>

      <ul>
        <% @event.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :users, style: "display: block" %>
    <%= form.collection_select :user_ids, User.all, :id, :name, {}, {multiple: true, id: 'category-select', class: "dropdown"} %>
  </div>

<br/>
  <div>
    <%= form.submit "Book Tickets" %>
  </div>
<% end %>
